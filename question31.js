"use strict";
//No users:
//Asuming you have an array of usernames
let users = ["user1", "user2", "user3"];
//if test to check the list is empty
if (users.length === 0) {
    console.log("we need to find users!");
}
else {
    users = [];
    console.log("all users have been removed.");
}
